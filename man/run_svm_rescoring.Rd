% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PSM-rescore.R
\name{run_svm_rescoring}
\alias{run_svm_rescoring}
\title{Re-score peptide-spectrum matches using an SVM}
\usage{
run_svm_rescoring(psm, kernel = "radial", cost = 1, gamma = NULL)
}
\arguments{
\item{psm}{A \link{PSM} object. PSM variables (spectrum, peptide,
protein, decoy, rank) must be set via \code{\link[=PSM]{PSM()}}; all remaining
numeric columns are used as SVM features.}

\item{kernel}{\code{character(1)} SVM kernel. One of \code{"radial"}
(default), \code{"linear"}, \code{"polynomial"}, or \code{"sigmoid"}.
Passed to \code{\link[e1071:svm]{e1071::svm()}}.}

\item{cost}{\code{numeric(1)} regularisation parameter \code{C}. Default
is \code{1}. Larger values impose a harder margin and may overfit.}

\item{gamma}{\code{numeric(1)} or \code{NULL}. Kernel coefficient for
radial, polynomial and sigmoid kernels. When \code{NULL} (default),
\code{1 / number_of_features} is used.}
}
\value{
A \code{list} with two elements:
\describe{
\item{\code{model}}{The fitted \code{svm} object returned by
\code{\link[e1071:svm]{e1071::svm()}}.}
\item{\code{rescored}}{The input \link{PSM} object with \code{svmScore}
(\code{numeric}, posterior target probability) and \code{svmRank}
(\code{integer}, within-peptide rank) columns appended.}
}
}
\description{
Top-level wrapper that prepares SVM input data from a \link{PSM}
object, trains a Support Vector Machine to discriminate target
from decoy PSMs, and rescores all PSMs with the resulting model.

The column names used at each step (decoy flag, peptide grouping)
are resolved from the PSM variables stored in the \link{PSM} object
via \code{\link[=psmVariables]{psmVariables()}}, so no hard-coded column names are assumed.

The workflow proceeds as follows:
\enumerate{
\item Extract feature columns — all columns not registered in
\code{\link[=psmVariables]{psmVariables()}} (spectrum, peptide, protein, decoy, rank,
score, fdr).
\item Remove zero-variance features; scale retained features to
zero mean and unit variance.
\item Encode target/decoy labels as \code{1} / \code{-1}.
\item Fit an \code{e1071} SVM with probability estimation enabled.
\item Predict posterior target probabilities; append \code{svmScore} and
\code{svmRank} columns to the \link{PSM} object.
}
}
\examples{
## ----------------------------------------------------------
## Build a small synthetic PSM object with two feature columns
## ----------------------------------------------------------
set.seed(42)
n <- 120L
psmDf <- data.frame(
    spectrumID      = paste0("sp", seq_len(n)),
    sequence        = sample(
        c("PEPTIDEA", "PEPTIDEB", "PEPTIDEC"),
        n, replace = TRUE
    ),
    protein         = sample(
        paste0("Prot", LETTERS[seq_len(5)]),
        n, replace = TRUE
    ),
    isDecoy         = sample(
        c(FALSE, TRUE), n,
        replace = TRUE, prob = c(0.7, 0.3)
    ),
    rank            = rep(1L, n),
    score           = runif(n),
    ## --- feature columns used by the SVM ---
    massErrorPpm    = rnorm(n, mean = 0, sd = 5),
    missedCleavages = sample(0:2, n, replace = TRUE)
)

## Wrap in a PSM object and register PSM variables
psm <- PSM(psmDf,
           spectrum = "spectrumID",
           peptide  = "sequence",
           protein  = "protein",
           decoy    = "isDecoy",
           rank     = "rank",
           score    = "score")
psm
psmVariables(psm)

## Run SVM rescoring with a radial kernel (default settings)
result <- run_svm_rescoring(psm)

## Inspect the rescored PSM object
rescored <- result$rescored
rescored[, c("sequence", "isDecoy", "score",
             "svmScore", "svmRank")]

## Retrieve the best-ranked target PSM per peptide
decoyCol <- psmVariables(rescored)["decoy"]
topHits <- rescored[
    !rescored[[decoyCol]] & rescored[["svmRank"]] == 1L,
]
topHits <- topHits[order(-topHits[["svmScore"]]), ]
topHits[, c("sequence", "protein", "score", "svmScore")]

## Tune the SVM with a linear kernel and higher cost
result2 <- run_svm_rescoring(psm, kernel = "linear", cost = 10)

}
